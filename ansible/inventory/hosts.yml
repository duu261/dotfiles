---
# Profile (desktop|tui) is chosen at run time with -e profile=desktop|tui,
# NOT via inventory groups - site.yml gates the desktop role on that var.
all:
  hosts:
    localhost:
      ansible_connection: local
